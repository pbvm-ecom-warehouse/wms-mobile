# Team Context

Ghi lại thông tin ổn định, hữu ích qua nhiều phiên làm việc.

## Working Notes

- Mục tiêu giai đoạn này là folder rõ ràng, role navigation đúng và giao diện đồng bộ; chưa cần làm sâu CRUD, API nghiệp vụ chi tiết hay flow từng phân hệ.
- Khi cần mở rộng tính năng, ưu tiên thêm từng phân hệ theo chiều dọc trên nền cấu trúc hiện tại thay vì refactor lại router hoặc theme.
- Người dùng muốn đẩy thay đổi lên `main` trực tiếp khi đã chốt, thường giữ commit nhỏ và tách phần unrelated khỏi commit chính.
- `.codex` được dùng như bộ nhớ làm việc của repo này và được phép commit.

Không lưu dữ liệu nhạy cảm trong file này.
